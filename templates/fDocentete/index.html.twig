{% extends 'base.html.twig' %}
{% block body %}
  <div class="wrap">
    <h1 class="wp-heading-inline">
      {{ 'Clients' | trans }}
    </h1>
    <a href="{{ getUrlWithParam('tab', 'new') }}" class="page-title-action">{{ 'Add New'|trans }}</a>
    <hr class="wp-header-end">
    <h2 class="screen-reader-text">{{ 'Filter clients list'|trans }}</h2>
    {% include 'fdocentete/filters.html.twig' %}
    {% include 'fdocentete/lists.html.twig' %}
    <div class="clear"></div>
  </div>
{% endblock %}
